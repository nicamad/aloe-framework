{
  "name": "aloe-framework",
  "version": "0.1.0",
  "private": true,
  "description": "A framework for defining, evaluating, and operating purpose-built agents through aloe.yaml.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
